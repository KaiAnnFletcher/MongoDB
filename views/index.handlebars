<div class="container">

        <div class="row">

            <div class="col-sm-12 scroll">
                <div class="card" style="height: 20rem">
                    <h1 class="card-header text-center">NewsGuard!</h1>
                    <div class="card-body text-center">
                    <div class="row">
                    <div class="col-sm" id="article-holder">
                        <h4>{{#each title}}</h4>
                        <p>{{#each summary}}</p>
                        <a>{{#each link}}</a>
                    </div>
                   
                </div>
                </div>
                
            </div>
        </div>
    </div>
</div>